This chapter will firstly go over the existing DIY solutions for 360° LiDAR platforms which the author has stumbled upon during the research[too informal?], analyzing their pros and cons.
After that, the proposed design will be presented, along with the chosen design decisions and their justifications.
\section{Existing DIY Solutions for Environmental Mapping}
\subsection{Using a Single-Point \gls{lidar} module}

One of the most straightforward approaches to achieve 360° environmental mapping is to utilize a single-point \gls{lidar} module mounted on a platform which is able to rotate and tilt. The implementation could be found in this project: \cite{lidar_design_1}.
\begin{figure}
    \centering
    \includegraphics[width=0.5\textwidth]{design/3D_1.png}
    \caption{Implementation in project \cite{lidar_design_1} using a single-point \gls{lidar}}
    \label{ph:lidar_design_1}
\end{figure}


\begin{tabular}{p{0.45\linewidth} p{0.45\linewidth}}
\textbf{Pros} & \textbf{Cons} \\
\begin{itemize}[label=\textcolor{green!60!black}{\textbf{+}}]
    \item Single-point \gls{lidar}s are the most cost-effective among all existing types
    \item Simple design
\end{itemize}
&
\begin{itemize}[label=\textcolor{red!70!black}{\textbf{--}}]
    \item Slow data acquisition relative to methods leveraging 2D \gls{lidar}'s speed
    \item Movement of lidar may introduce innacuracies in distance measurements, which need to be compensated for in code if we want to assume that all points are measured from a single position in space
\end{itemize}
\end{tabular}


\subsection{Using a Single-Point \gls{lidar} and a Mirror}
The following approach was used in this project: \cite{lidar_design_2}. A single-point \gls{lidar} is placed below a rotating mirror, which reflects the laser beam horizontally. As the mirror rotates, the laser beam scans the environment in 360$\degree$. Moreover, by tilting the mirror at an angle, vertical scanning can also be achieved, allowing for the acquisition of the whole scene.

\begin{figure}
    \centering
    \includegraphics[width=0.75\textwidth]{design/3D_2.jpg}
    \caption{Implementation in project \cite{lidar_design_2} using a single-point \gls{lidar} and a mirror}
    \label{ph:lidar_design_2}
\end{figure}

\begin{tabular}{p{0.45\linewidth} p{0.45\linewidth}}
\textbf{Pros} & \textbf{Cons} \\
\begin{itemize}[label=\textcolor{green!60!black}{\textbf{+}}]
    \item Cost-effectiveness as in previous implementation
    \item Simple design
    \item Much easier to correct the distance measurement, since the \gls{lidar} is stationary and the distance from it to the mirror is known.
\end{itemize}
&
\begin{itemize}[label=\textcolor{red!70!black}{\textbf{--}}]
    \item Slow data acquisition relative to methods leveraging 2D \gls{lidar}'s speed
    \item Mirror's quality may affect the accuracy of distance measurements
    \item Mechanical structure holding the mirror may introduce vibrations, thus affecting measurement accuracy
\end{itemize}
\end{tabular}


\subsection{Using a 2D \gls{lidar} Mounted on a Tilting Platform}
\label{sec:tilting_platform}
This approach utilizes a 2D \gls{lidar} sensor mounted on a platform that can tilt the sensor side-to-side, as demonstrated in this project: \cite{lidar_design_3}. The 2D \gls{lidar} scans the environment in a horizontal plane, while the tilting mechanism allows for vertical scanning.

\begin{figure}
    \centering
    \includegraphics[width=0.7\textwidth]{design/3D_3.jpg}
    \caption{Implementation in project \cite{lidar_design_3} using a tilting platform}
    \label{ph:lidar_design_3}
\end{figure}

\begin{tabular}{p{0.45\linewidth} p{0.45\linewidth}}
\textbf{Pros} & \textbf{Cons} \\
\begin{itemize}[label=\textcolor{green!60!black}{\textbf{+}}]
    \item Faster data acquisition compared to solutions with single-point \gls{lidar} measurements
    \item Higher resolution given by the use of a 2D \gls{lidar} sensor
    \item This design could be potentially used for \gls{slam} applications, given the higher speed
\end{itemize}
&
\begin{itemize}[label=\textcolor{red!70!black}{\textbf{--}}]
    \item More expensive than single-point \gls{lidar} solutions
    \item Point distribution is non-uniform due to the tilting mechanism which produces denser point clouds on the axis of tilting (see \cref{fig:tilt_pointCloud})
    \item Mechanical design is more prone to vibrations produced by the \gls{lidar} itself affecting measurement accuracy
\end{itemize}
\end{tabular}
\begin{figure}[H]
    \centering
    \input{design/tilt_pointCloud.tex}
    \caption{Simulation of an obtained point cloud using a tilting platform}
    \label{fig:tilt_pointCloud}
\end{figure}

\section{Proposed Design of a 360$\degree$ \gls{lidar} Platform}
Based on the analysis of existing DIY solutions for 360° \gls{lidar} platforms, the proposed design for this miniproject involves using a 2D \gls{lidar} sensor mounted on a tilted platform that is able to rotate around its axis.
\begin{figure}[H]
    \caption{Proposed platform design. \protect$\theta$ is the tilt angle}
    \input{design/tikz_platform.tex}
    \label{fig:tikz_platform}
\end{figure}
This design leverages the advantages of 2D \gls{lidar} sensors, such as fast data acquisition and high resolution, while also eliminating some of the drawbacks of previous designs, namely:
\begin{itemize}[label=\textcolor{green!60!black}{\textbf{+}}]
    \item The platform will be quite rigid, minimizing vibrations
    \item Mechanical design is very simple, making it easy to build and troubleshoot
    \item The resulting point cloud will be more uniformly distributed compared to the one obtained using tilting platform design presented in \cref{sec:tilting_platform} unless the tilt angle is extremely steep (see \cref{fig:rotate_pointCloud})
\end{itemize}
\begin{figure}[h!]
    \centering
    
    \begin{subfigure}{0.45\linewidth}
        \centering
        \input{design/30rotate_pointCloud.tex}
        \caption{$\theta=30\degree$}
        \label{fig:rotate_pointCloud:30}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.45\linewidth}
        \centering
        \input{design/60rotate_pointCloud.tex}
        \caption{$\theta=60\degree$}
        \label{fig:rotate_pointCloud:60}
    \end{subfigure}

    \caption{Simulation of an obtained point cloud using a rotating tilted platform with two different tilt angles $\theta$}
    \label{fig:rotate_pointCloud}
\end{figure}

Such design does have its drawbacks, however. The first one is immediately apparent from \cref{fig:rotate_pointCloud}: there are blind spots in the point cloud where no points are measured.
While these blind spots can be minimized by choosing as large a tilt angle as possible, it would reintroduce the problem of non-uniform point cloud density.

Besides that, compared to the tilting platform design presented in \cref{sec:tilting_platform}, the proposed design would need to rotate twice as much to obtain all the full coverage of the environment, 
since the tilting platform scanned only within $[-90\degree, +90\degree]$, whereas the current design must complete a full $360\degree$. This would lead to longer scanning times.

\subsection{Controlling the Platform}
Since we want our platform to be precisely controlled, we need to choose an appropriate motor.
Stepper motors are commonly used in applications requiring precise positioning, as they can be controlled to move in small, discrete steps.
For this design, a NEMA 17 17HS8401 stepper motor is used to rotate the \gls{lidar} platform.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{design/stepper_motor_photo.jpg}
    \caption{NEMA 17 17HS8401 photo\protect\footnotemark[2]}
    \label{ph:lidar_design_1}
\end{figure}
\begin{table}[H]
    \centering
    \caption{Crucial parameters of the chosen NEMA 17 17HS8401 stepper motor\protect\footnotemark[2]}
    \rowcolors{0}{ctubluebg!65}{ctubluebg!20}
    \begin{tabular}{|c|c|}
        \toprule
        Motor Weight [$\ohm$] & 360 \\
        Holding Torque [$\si{N.m}$] & 0.5\\
        Step Angle [$\degree$] & 1.8 \\
        \# of Steps per Revolution [-] & 200 \\
        Rated Current [$\si{A}$] & 1.68 \\
        \bottomrule
    \end{tabular}
    \label{tab:stepper_motor_params}
\end{table}
\footnotetext[2]{Photo and parameters were taken from \url{https://www.laskakit.cz/en/krokovy-motor-nema-17-17hs8401-0-5nm/}}
For heavy load applications, it is important to consider the potential error sources of stepper motors, such as the stepper motor losing steps due to a heavy load or by driving the motor faster than its maximum speed \cite[p.~13]{friedl_SpeedControlStepper_2008}.
However, in our case, the load will be relatively light for this motor and the speed will be kept low, thus a feedback mechanism for correcting lost steps is not required.
\subsection{Choosing the Tilt Angle}
The tilt angle of the \gls{lidar} platform is a crucial parameter that affects the size of the aforementioned blind spots in the point cloud (see \cref{fig:rotate_pointCloud}).
A larger tilt angle will result in smaller blind spots as could be seen from the simulations below.

The trade-off for a larger tilt angle, however, is a more spread-out point cloud in the horizontal direction if we rotate our platform in discrete steps [see point cloud simulations below].
Without any further solutions, we would have to choose a tilt angle that balances the size of blind spots and point cloud density.

Thankfully, there is a way to achieve a more complete coverage of the environment without sacrificing point cloud density.
\subsection{Choosing a Gear Ratio for the Rotating Platform}
In order to remove the need for a compromise between point cloud coverage and density while using a stepper motor to rotate the platform, we can introduce a gear mechanism into our design.
Using gears with a specific gear ratios allows us to choose a tilt angle that minimizes blind spots while still maintaining a dense point cloud.






